# 팀노바 골프 아카데미 웹사이트 — 배포 안내

## 🌐 현재 운영 중인 주소

**https://goltimjang.github.io/teamnova-golf-academy/**

GitHub Pages(무료)로 배포되어 있습니다. 파일을 수정한 뒤
`git add -A && git commit -m "수정" && git push` 하면 1~2분 내로 반영됩니다.
나중에 도메인(예: teamnovagolf.com)을 구입하면 GitHub 저장소 Settings → Pages → Custom domain에서 연결할 수 있습니다.

## 폴더 구성

```
teamnova-website/
├── index.html          ← 메인 페이지 (파일 하나로 전체 사이트)
├── css/style.css       ← 디자인 스타일
├── js/main.js          ← 애니메이션·인터랙션
├── assets/
│   ├── img/            ← 웹 최적화된 사진 (원본 대비 약 1/10 용량)
│   ├── video/          ← 훈련 영상 5개 (train-1 ~ train-5)
│   └── logo/           ← TN 로고 SVG, FITOWN 로고
└── 배포안내.md          ← 이 문서
```

## 배포 방법 (아무거나 하나 선택)

1. **Netlify (추천, 무료)** — https://app.netlify.com/drop 접속 후
   `teamnova-website` 폴더를 통째로 드래그하면 끝. 주소가 바로 발급됩니다.
2. **Vercel** — vercel.com 가입 후 폴더 업로드.
3. **일반 호스팅(카페24 등)** — 폴더 내용물을 FTP로 `public_html`에 업로드.

커스텀 도메인(예: teamnovagolf.com)은 호스팅 서비스의 도메인 연결 메뉴에서 설정하면 됩니다.

## 나중에 꼭 교체할 것

- **카카오톡 채널 링크** — 현재 임시 링크입니다. 채널 개설 후
  `index.html`에서 `pf.kakao.com/_teamnovagolf` 를 전부 찾아 실제 채널 주소로 바꾸세요 (총 4곳).
- 수강료 정책이 바뀌면 FAQ의 수강료 답변과 프로그램 카드 문구를 수정하세요.

## 수정 팁

- 전화번호: `index.html`에서 `010-8483-9113` 검색 후 일괄 변경
- 사진 교체: `assets/img/` 안의 파일을 같은 이름으로 덮어쓰기
- 영상 교체: `assets/video/train-N.mp4` 같은 이름으로 덮어쓰기 (세로 9:16 권장)
